package it.stacja.springworkshop100520;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Fail.fail;

// TODO: write all other tests for the service
class TweetServiceIT {

	// @formatter:off
	@DisplayName(
		"given hi, hello and welcome tweets, " +
		"when find by msg hello, " +
		"then only hello is tweet is found"
	)
	//@formatter:on
	@Test
	void findByMsg() throws Exception {
		// TODO: write test
		// given

		// when

		// then
		fail("Write your test");
	}
}
